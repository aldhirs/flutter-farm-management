<?xml version="1.0" encoding="utf-8"?>
<!-- Debug builds only. Allows plain HTTP to a locally hosted API:
     - localhost / 127.0.0.1 : reachable over `adb reverse tcp:8080 tcp:8080`
     - 10.0.2.2              : the host machine as seen from an Android emulator
     Everything else still requires HTTPS. -->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
    </domain-config>
</network-security-config>
