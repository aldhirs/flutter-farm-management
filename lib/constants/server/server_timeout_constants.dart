class ServerTimeoutConstants {
  const ServerTimeoutConstants._();
  static const connectTimeout = Duration(seconds: 3600);
  static const receiveTimeout = Duration(seconds: 3600);
  static const sendTimeout = Duration(seconds: 3600);
}
